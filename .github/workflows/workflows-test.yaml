name: Test Work Flow

on: 
  workflow_dispatch:
    inputs:
        version:
          description: 'Version to deploy'
          required: true
  repository_dispatch:
    types: [test-event]
          
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test step
        id: test-step
        run: |
          echo "Deploying ${{ inputs.version }}"
