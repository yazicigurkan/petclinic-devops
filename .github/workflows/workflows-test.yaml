name: Test Work Flow

on: 
  repository_dispatch:
    types: [test-event]
          
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test step
        run: echo "Gürkan" |
          echo "${{ github.event.client_payload.jira_issue }}"
