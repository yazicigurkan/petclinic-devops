name: Test Work Flow

on: 
  workflow_dispatch:
    inputs:
        version:
          description: 'Version to deploy'
          required: true
          
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test step
        run: |
          echo "Deploying ${{ inputs.version }}"
    
        
