apiVersion: v1
kind: Service
metadata:
  name: petclinic
  namespace: default
spec:
  type: NodePort
  selector:
    app: petclinic
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
